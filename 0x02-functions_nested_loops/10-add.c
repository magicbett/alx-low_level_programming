#include "main.h"
/**
* add - a function that adds two integers
* and returns the result.
*
* @x: number one.
* @y: number two.
*
* Return: Add of number one and number two.
*/
int add(int x, int y)

{
	return (x + y);
}
